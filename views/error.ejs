<%- layout('layouts/boilerplate') %>

<div class="row mt-3">
  <div class="alert alert-danger" role="alert">
    <h4 class="alert-heading"><%= err.message || "Something went wrong!" %></h4>
    <p><%= err.stack || "No error trace available." %></p>
  </div>
</div>
